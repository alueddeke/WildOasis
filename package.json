{
  "name": "The Wild Oasis",
  "version": "1.0.0",
  "description": "A full-stack application to manage the bookings of a private AirBnB",
  "scripts": {
    "start": "npm run start --prefix backend",
    "build": "npm run build --prefix client",
    "postinstall": "npm install --prefix backend && npm install --prefix client"
  },
  "dependencies": {},
  "devDependencies": {}
}
